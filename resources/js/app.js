import React from 'react';
import ReactDOM from 'react-dom';
import DossierMedical from './components/DossierMedical';

if (document.getElementById('root')) {
    ReactDOM.render(<DossierMedical />, document.getElementById('root'));
}